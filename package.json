{
  "name": "inkpersona",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "frontend:install": "cd frontend && npm install",
    "frontend:test": "cd frontend && npm test",
    "frontend:build": "cd frontend && npm run build",
    "gradio:test": "uv run pytest tests",
    "backend:test": "uv run pytest backend",
    "test": "npm run gradio:test && npm run backend:test && npm run frontend:test && npm run frontend:build"
  }
}
